#include<stdio.h>
#include<math.h>
#include<iostream>

char  a;
int   b;
float c;

void main() // Funcion principal
{
    a = (int)((int)(c) + (float)(b));
    c = 18.9;
    a = 65537;
    b = 9;

    c = (int)((int)(c) + (float)(1));
    
}
